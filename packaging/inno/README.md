# Inno Setup installer — Phase 8

Package `dist/portable/QwenSubtitle` with Inno Setup 7 + disk spanning
(`Setup.exe` + `.bin` slices) because the full offline tree exceeds 4 GB.

License review checklist before commercial release is in the Engineering Spec §39.
